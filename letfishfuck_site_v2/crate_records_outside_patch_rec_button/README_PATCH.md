# CRATE RECORDS OUTSIDE — Patch
- Adds bottom micro button: **REC**
  - scrolls to transport
  - attempts to start recording (fails gracefully if permissions missing)

Notes for Cathedral lockbox:
- Default = GEO
- Purge? = ME (manual only)
